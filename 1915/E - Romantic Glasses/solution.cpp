#include<bits/stdc++.h>using namespace std;typedef long long ll;void f(ll n,vector<ll>&a){    for(ll i=0;i<n;i++)cin>>a[i];}void solve(){    ll n;    cin>>n;    vector<ll>arr(n);    for (ll i = 0; i < n; i++)    {       cin>>arr[i];       if(i&1)arr[i]*=-1;    }        map<ll,ll>mp;    mp[0]=1;    ll sum=0;    for (ll i = 0; i < n; i++)    {        sum+=arr[i];        if(mp[sum]){            cout<<"YES"<<'\n';            return;        }        mp[sum]++;     }    cout<<"NO"<<'\n';        } int main(){ios::sync_with_stdio(0);cin.tie(0);ll t;cin>>t;while (t--){    solve();}     return 0;}