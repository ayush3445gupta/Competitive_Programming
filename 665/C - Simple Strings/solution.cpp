#include<bits/stdc++.h>using namespace std;typedef long long ll;void f(ll n,vector<ll>&a){    for(ll i=0;i<n;i++)cin>>a[i];}void solve(){    string s;    cin>>s;    ll n=s.length();    for (ll i = 1; i < n; i++)    {        if(s[i]==s[i-1]){            for(char ch='a';ch<='z';ch++){                if(ch==s[i-1])continue;                if(i+1<n && ch==s[i+1])continue;                                s[i]=ch;                break;            }        }    }    cout<<s<<'\n';} int main(){ios::sync_with_stdio(0);cin.tie(0);solve();    return 0;}