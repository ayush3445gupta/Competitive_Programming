// #include<bits/stdc++.h>// using namespace std;// typedef long long ll;// void f(ll n,vector<ll>&a){//     for(ll i=0;i<n;i++)cin>>a[i];// }// ll gcd_of_differences(vector<ll> &a) {//     ll g = 0;//     for (int i = 1; i < a.size(); i++) {//         g = __gcd(g, a[i] - a[i - 1]);//     }//     return g;// }// void solve(){//     ll n;//     cin>>n;//     vector<ll>arr(n);//     f(n,arr);//     if(n==1){//         cout<<1<<'\n';//         return;//     }//     sort(begin(arr),end(arr));//     ll diff=gcd_of_differences(arr);//         ll k=1;//         ll x=arr[n-1];//         for (ll i = n-2; i>=0; i--)//         {//             if(arr[n-1]-arr[i]==k*diff)k++;//             else {//                 x=arr[n-1]-k*diff;//                 break;//             }//         }//         ll ans2=0;//         if(x==arr[n-1]){//             x=arr.back()+diff;//             for(ll i:arr)ans2+=(x-i)/diff;//             cout<<ans2<<'\n';//         }//         else{//             for(ll i:arr)ans2+=(arr[n-1]-i)/diff;//             cout<<ans2+(arr[n-1]-x)/diff<<'\n';        //         }    // } // int main(){// ll t;// cin>>t;// while (t--)// {//     solve();// } //     return 0;// }  #include<bits/stdc++.h>using namespace std;typedef long long ll;void f(ll n,vector<ll>&a){    for(ll i=0;i<n;i++)cin>>a[i];}void solve(){    ll n;    cin>>n;    vector<ll>arr(n);    f(n,arr);    if(n==1)cout<<1<<'\n';    else{        sort(begin(arr),end(arr));        ll gd=0;        for (ll i = 1; i < n; i++)        {            ll diff=abs(arr[i]-arr[i-1]);            gd=__gcd(diff,gd);        }        ll k=1;        ll x=arr[n-1];        for (ll i=n-2;i>=0;i--) {            if (arr[n-1]-arr[i]==k*gd)k++;            else {                x=arr[n-1]-k*gd;                break;            }        }        ll ans=0;        for(ll i:arr)ans+=(arr[n-1]-i)/gd;        if(x==arr[n-1])ans+=n;        else ans+=(arr[n-1]-x)/gd;        cout<<ans<<'\n';    }} int main(){ios::sync_with_stdio(0);cin.tie(0);ll t;cin>>t;while (t--){    solve();}     return 0;}