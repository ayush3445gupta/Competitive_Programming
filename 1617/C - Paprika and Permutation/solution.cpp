#include<bits/stdc++.h>using namespace std;typedef long long ll;void f(ll n,vector<ll>&a){    for(ll i=0;i<n;i++)cin>>a[i];}// remainder for n = [1,n-1/2]// void solve(){//     ll n;//     cin>>n;//     vector<ll>arr(n);//     f(n,arr);//     ll cnt=0;//     vector<ll>diff_arr(n+2,0);//     unordered_set<ll>st;//     for (ll i = 0; i < n; i++)//     {//         if(arr[i]<=n && st.find(arr[i])==st.end()){//             st.insert(arr[i]);//         }//         else if (arr[i]!=2)//         {//             cnt++;//             diff_arr[1]++;//             diff_arr[min(n+1,1+(arr[i]-1)/2)]--;//         }  //     }//     for (ll i = 1; i<=n; i++){//         diff_arr[i]+=diff_arr[i-1];//         if(st.find(i)!=st.end()){//             diff_arr[i]+=1;//             diff_arr[i+1]-=1;//         }        //         if(diff_arr[i]<=0){//             cout<<-1<<'\n';//             return;//         }//     }//     cout<<cnt<<'\n';// } void solve(){    ll n;    cin>>n;    set<ll>st;    for (ll i = 1; i <=n; i++)st.insert(i);    vector<ll>arr;    for (ll i = 0; i < n; i++)    {        ll a;        cin>>a;        if(st.find(a)!=st.end())st.erase(a);        else arr.push_back(a);    }    ll cnt=0;    sort(rbegin(arr),rend(arr));    for(auto& it:arr){        auto k=st.end();        k--;        ll num=(*k);        if(num>(it-1)/2){            cout<<-1<<'\n';            return;        }        st.erase(k);    }            cout<<arr.size()<<'\n';    }int main(){ll t;cin>>t;while (t--){    solve();}     return 0;}