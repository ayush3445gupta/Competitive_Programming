#include<bits/stdc++.h>using namespace std;typedef long long ll;void func(ll n,vector<ll>&a){    for(ll i=0;i<n;i++)cin>>a[i];}// multiset<pair<ll,ll>>mst;// void solve(){//     mst.clear();//     ll n,m;//     cin>>n>>m;//     vector<vector<ll>>arr(n,vector<ll>(m));//         for (ll i = 0; i < n; i++)//         {//             for (ll j = 0; j < m; j++)//             {//                 arr[i][j]=-1;//                 ll x;//                 cin>>x;//                 mst.insert({x,i});//             }//         }    //     // fill smallest m values//     for (ll i = 0; i < m; i++)//     {//        auto it=mst.begin();//        arr[it->second][i]=it->first;//        mst.erase(it);//     } //     while (!mst.empty())//     {//         auto it=mst.begin();//         ll in=0;//         while (arr[it->second][in]!=-1)//             in++;        //         arr[it->second][in]=it->first;//         mst.erase(it);//     }//     for (ll i = 0; i < n; i++)//     {//        for (ll j = 0; j < m; j++)cout<<arr[i][j]<<" ";//        cout<<'\n';//     }// }void solve(){        ll n,m;    cin>>n>>m;    vector<pair<ll,ll>>vec;   for (ll i = 0; i < n; i++)   {    for (ll j = 0; j < m; j++)    {        ll a;        cin>>a;        vec.push_back({a,i});    }   }   sort(begin(vec),end(vec));   vector<vector<ll>>res(n,vector<ll>(m,-1));   vector<ll>ptr(n,0);   for (ll i = 0; i < m; i++)   {     ll a=vec[i].first,b=vec[i].second;     res[b][i]=a;   }   for (ll i = m; i < (ll)vec.size(); i++)   {    ll row=vec[i].second;    ll val=vec[i].first;    while (res[row][ptr[row]]!=-1)    {        ptr[row]++;    }    res[row][ptr[row]]=val;   }   for (ll i = 0; i < n; i++)   {    for (ll j = 0; j < m;j++)    {        cout<<res[i][j]<<" ";     }    cout<<'\n';   }   }int main(){ll t;cin>>t;while (t--){    solve();}     return 0;}