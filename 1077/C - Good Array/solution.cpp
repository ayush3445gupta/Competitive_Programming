#include<bits/stdc++.h>using namespace std;typedef long long ll;void f(ll n,vector<ll>&a){    for(ll i=0;i<n;i++)cin>>a[i];}void solve(){    ll n;    cin>>n;    vector<ll>arr(n);    unordered_map<ll,ll>mp;    unordered_set<ll>st;    for (ll i = 0; i < n; i++)    {        cin>>arr[i];        mp[arr[i]]++;    }    ll sum=accumulate(begin(arr),end(arr),0ll);        vector<ll>ans;    for (ll i = 0; i < n; i++)    {        ll req=sum-arr[i];        if(req%2==0){            if(arr[i]==(req/2))mp[req/2]--;            if(mp[req/2]>0)ans.push_back(i+1);            if(arr[i]==(req/2))mp[req/2]++;        }    }    cout<<ans.size()<<'\n';    for(ll i:ans)cout<<i<<" ";    cout<<'\n';} int main(){ios::sync_with_stdio(0);cin.tie(0);solve();    return 0;}